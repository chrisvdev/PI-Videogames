# .env File

```javascript
DB_USER=usuariodepostgres
DB_PASSWORD=passwordDePostgres
DB_HOST=localhost
PORT=8080 # si no se declara por defecto es el 80
CORS=DISABLED # o si pones el dominio espera que ingresen las peticiones desde ese dominio, por defecto es http://localhost:PORT
API_KEY=RAWG_API_KEY
```
